```{=html}
<div class="teasers">
<% for (const item of items) { %>
  <div class="teaser" <%= metadataAttrs(item) %>>
    <h3><a href="<%= item.path %>"><%- item.title %></a></h3>
    <% if (item.description) { %><p><%- item.description %></p><% } %>
  </div>
<% } %>
</div>
```
