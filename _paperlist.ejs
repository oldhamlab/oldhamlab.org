```{=html}
<%
// Quarto has already sorted by date descending; grouping preserves that order.
const years = [];
const byYear = {};
for (const item of items) {
  const y = item.date ? new Date(item.date).getUTCFullYear() : '';
  if (!(y in byYear)) { byYear[y] = []; years.push(y); }
  byYear[y].push(item);
}
%>
<div class="pubs">
<% for (const y of years) { %>
  <section class="pub-year">
    <h2 class="pub-year-label"><%= y %></h2>
    <div class="pub-list">
    <% for (const item of byYear[y]) { %>
      <article class="pub" <%= metadataAttrs(item) %>>
        <h3 class="pub-title"><% if (item.path) { %><a href="<%= item.path %>"><%= item.title %></a><% } else { %><%= item.title %><% } %></h3>
        <p class="pub-authors"><%= item.author %></p>
        <p class="pub-source"><em><%= item.journal %></em><% const sep = /\.$/.test(item.journal) ? ' ' : '. '; %><% if (item.volume) { %><%= sep %><%= item.volume %><% if (item.pages) { %>:<%= item.pages %><% } %><% } else if (item.pages) { %><%= sep %><%= item.pages %><% } %><% if (item.preprint) { %> <span class="pub-tag">Preprint</span><% } %><% if (item.chapter) { %> <span class="pub-tag">Chapter</span><% } %></p>
        <% if (item.award) { %><p class="pub-award"><%= item.award %></p><% } %>
        <% if (item.equal) { %><p class="pub-note"><%= item.equal %></p><% } %>
        <% if (item.note) { %><p class="pub-note"><%= item.note %></p><% } %>
      </article>
    <% } %>
    </div>
  </section>
<% } %>
</div>
```
